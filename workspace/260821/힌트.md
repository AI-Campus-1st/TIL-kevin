## 1. 네이버 VIBE Top 100 수집

1. 요청 Header에 Accept를 찾아 넣어주세요.

## 2. 삼성전자 일별 시세 1년치 수집

1. 일별 시세 페이지를 바꿔보면 문서가 들어오는데 `Fetch/XHR` 탭에 있지 않고 `Doc` 탭에 들어옵니다.

## 3. 네이버 뉴스 검색기 함수 만들기 (스크롤 포함)

1. 하나의 뉴스를 포함하는 태그는 `div.fds-news-item-list-tab > div` 형태입니다. 뉴스 아이템 리스트 내부에 자식으로 바로 붙어있는 div 태그를 반복문으로 돌려서 각 요소들을 가져옵니다.
